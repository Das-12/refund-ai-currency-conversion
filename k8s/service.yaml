apiVersion: v1
kind: Service
metadata:
  name: currency-service
  namespace: refundai
spec:
  selector:
    app: currency-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP  