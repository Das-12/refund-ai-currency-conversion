apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-config
  namespace: infra
data:
  mongod.conf: |
    storage:
      dbPath: /data/db
    net:
      port: 27017
      bindIp: 0.0.0.0
    security:
      authorization: enabled
