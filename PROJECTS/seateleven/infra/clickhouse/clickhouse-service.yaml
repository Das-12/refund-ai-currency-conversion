apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: infra
  labels:
    app: clickhouse
spec:
  ports:
  - port: 8123
    name: http
    protocol: TCP
  - port: 9000
    name: native
    protocol: TCP
  clusterIP: None
  selector:
    app: clickhouse
